# Environment variables for javascript_playground_frontend

# PUBLIC_INTERFACE
# Base URL for backend API used for sharing and code execution.
# The orchestrator will set these in the real .env; do not commit actual values here.
REACT_APP_API_BASE_URL=https://your-backend.example.com
REACT_APP_SITE_URL=http://localhost:3000
